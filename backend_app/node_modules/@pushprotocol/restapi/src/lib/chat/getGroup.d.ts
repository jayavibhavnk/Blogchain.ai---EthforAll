import { AccountEnvOptionsType, IGroup } from '../types';
/**
 *  GET /v1/chat/groups/:chatId
 */
export interface GetGroupType extends AccountEnvOptionsType {
    chatId: string;
}
export declare const getGroup: (options: GetGroupType) => Promise<IGroup>;
