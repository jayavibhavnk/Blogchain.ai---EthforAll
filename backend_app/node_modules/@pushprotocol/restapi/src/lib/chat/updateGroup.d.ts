import { AccountEnvOptionsType } from '../types';
/**
 *  PUT /v1/chat/groups/:chatId
 */
export interface ChatUpdateGroupType extends AccountEnvOptionsType {
    chatId: string;
    groupName: string;
    profilePicture: string;
    members: Array<string>;
    admins: Array<string>;
    address: string;
    pgpPrivateKey?: string;
}
export declare const updateGroup: (options: ChatUpdateGroupType) => Promise<any>;
